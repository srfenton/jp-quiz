<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submit Answer</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* General Body Styling */
        body {
            font-family: "Playfair Display", serif; /* Luxurious serif font */
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            background-color: #f9f7f2; /* Light cream background */
            color: #1a1a1a; /* Dark text */
            display: flex;
            flex-direction: column;
            height: 100vh; /* Full viewport height */
        }

        /* Top Section */
        .top-section {
            text-align: center;
            margin-top: 20px;
        }

        h1 {
            font-size: 2.5rem; /* Large, clear text */
            font-weight: 600;
            margin: 10px 0;
            color: #333;
        }

        /* Centered Answer Section */
        .center-section {
            flex-grow: 1; /* Takes up remaining space */
            display: flex;
            flex-direction: column;
            padding-left: 10%;
            padding-right: 10%;
            text-align: center;
            align-items: center;
            justify-content: center;
            transform: translateY(-20%);
        }

        p {
            font-size: 1.5rem; /* Larger for readability */
            margin: 10px 0;
            color: #555; /* Slightly muted color */
        }

        /* Footer Buttons */
        .footer-buttons {
            display: flex;
            justify-content: center;
            gap: 20px; /* Spacing between buttons */
            margin-bottom: 20px;
        }

        .footer-button {
            background-color: transparent;
            border: 2px solid #b2985b; /* Gold border */
            color: #1a1a1a; /* Dark text */
            padding: 10px 20px;
            font-size: 1rem;
            cursor: pointer;
            border-radius: 4px; /* Rounded corners */
            text-decoration: none;
            transition: all 0.3s ease-in-out;
            font-family: "Playfair Display", serif; /* Consistent font */
        }

        .footer-button:hover {
            background-color: #b2985b; /* Gold background on hover */
            color: #fff; /* White text */
        }
    </style>
</head>
<body>
    <!-- Question Section -->
    <div class="top-section">
        <h1><%= result %></h1>
    </div>
    
    <!-- Correct Answer Section -->
    <div class="center-section">
        <p><%= correctAnswer %> is the correct answer</p>
    </div>

    <!-- Footer Buttons -->
    <div class="footer-buttons">
        <form action='/quiz/question/' method="GET">
            <button type="submit" class="footer-button" id="button-0">NEXT</button>
        </form>

        <form action='/reset' method="POST">
            <button type="submit" class="footer-button">reset & go home</button>
        </form>
    </div>

    <script>
        // Automatically click the NEXT button after 1 second
        setTimeout(function() {
            document.getElementById('button-0').click(); 
        }, 500);
    </script>
</body>
</html>
